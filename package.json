{
  "name": "vendorbridge-root",
  "version": "1.0.0",
  "description": "VendorBridge Root package to run both frontend and backend concurrently.",
  "scripts": {
    "install:all": "npm --prefix Backend install && npm --prefix Frontend install",
    "start:backend": "npm --prefix Backend install && node Backend/index.js",
    "start:frontend": "npm --prefix Frontend install && npm --prefix Frontend run dev",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
